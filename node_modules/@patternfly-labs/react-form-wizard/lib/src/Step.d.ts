import { ReactNode } from 'react';
import { HiddenFn } from './inputs/Input';
export declare function Step(props: {
    label: string;
    children?: ReactNode;
    id: string;
    hidden?: HiddenFn;
    autohide?: boolean;
}): JSX.Element;

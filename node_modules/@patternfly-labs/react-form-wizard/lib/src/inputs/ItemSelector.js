import { jsx as _jsx } from "react/jsx-runtime";
import get from 'get-value';
import { Fragment, useContext } from 'react';
import { ItemContext } from '../contexts/ItemContext';
export function wizardSelectorItem(props, item) {
    return item.find((i) => {
        return get(i, props.selectKey) === props.selectValue;
    });
}
export function ItemSelector(props) {
    const item = useContext(ItemContext);
    if (!Array.isArray(item))
        return _jsx(Fragment, { children: "Input must be an array!" }, void 0);
    const newItem = wizardSelectorItem(props, item);
    if (newItem === undefined) {
        if (props.empty)
            return _jsx(Fragment, { children: props.empty }, void 0);
        return _jsx(Fragment, { children: "Item not found!" }, void 0);
    }
    return _jsx(ItemContext.Provider, { value: newItem, children: props.children }, void 0);
}
//# sourceMappingURL=ItemSelector.js.map
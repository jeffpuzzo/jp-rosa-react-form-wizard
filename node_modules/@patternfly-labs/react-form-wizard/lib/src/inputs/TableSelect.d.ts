import { ReactNode } from 'react';
import { InputCommonProps } from './Input';
interface ITableColumn<T> {
    name: string;
    cellFn: (item: T) => ReactNode;
}
export declare type TableSelectProps<T> = InputCommonProps<string> & {
    label: string;
    columns: ITableColumn<T>[];
    items: T[];
    itemToValue?: (item: T) => unknown;
    valueMatchesItem?: (value: unknown, item: T) => boolean;
    emptyMessage: string;
    summaryList?: boolean;
};
export declare function TableSelect<T = any>(props: TableSelectProps<T>): JSX.Element;
export {};
